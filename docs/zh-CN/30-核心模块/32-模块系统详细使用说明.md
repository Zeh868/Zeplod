# 模块系统详细使用说明

## 目录

- [概述](#概述)
- [应用启动与初始化顺序（Zephyr SYS_INIT）](#应用启动与初始化顺序zephyr-sys_init)
- [核心概念](#核心概念)
- [系统架构](#系统架构)
- [配置选项](#配置选项)
- [运行时依赖（可选）](#运行时依赖可选)
- [完整 Kconfig 索引](#完整-kconfig-索引)
- [API 参考](#api-参考)
- [使用指南](#使用指南)
- [最佳实践](#最佳实践)
- [故障排除](#故障排除)

---

## 概述

模块系统提供了一个动态的、可扩展的组件管理框架，支持模块的注册、生命周期管理和事件驱动通信。该系统与事件系统紧密集成，为应用程序提供模块化的架构支持。

### 主要特性

| 特性 | 描述 |
|------|------|
| 最大模块数 | 16 (可配置) |
| 每模块最大事件订阅 | 8 (可配置) |
| 初始化超时 | 1000ms (可配置) |
| 优先级调度 | 支持（4 个优先级） |
| 动态注册/注销 | 支持 |
| 模块状态管理 | 完整状态机 |
| 运行时依赖排序（可选） | `depends_on` + Kconfig，见下文 |

### 模块状态机

```
                    ┌──────────────────┐
                    │ UNINITIALIZED    │
                    │ (未初始化)        │
                    └────────┬─────────┘
                             │ register()
                             ▼
                    ┌──────────────────┐
                    │ INITIALIZING     │
                    │ (初始化中)        │
                    └────────┬─────────┘
                             │ init() 完成
                             ▼
                    ┌──────────────────┐
              ┌─────│ INITIALIZED      │─────┐
              │     │ (已初始化)        │     │
              │     └────────┬─────────┘     │
              │              │ start()       │ stop()
              │              ▼               │
              │     ┌──────────────────┐     │
              │     │ RUNNING          │◄────┤
              │     │ (运行中)          │     │
              │     └────────┬─────────┘     │
              │              │ suspend()     │
              │              ▼               │
              │     ┌──────────────────┐     │
              └────►│ SUSPENDED        │─────┘
                    │ (挂起)            │
                    └────────┬─────────┘
                             │ error
                             ▼
                    ┌──────────────────┐
                    │ ERROR            │
                    │ (错误)            │
                    └──────────────────┘
```

**挂起（suspend）语义**

- `module_manager_suspend_module` 仅将管理器状态设为 `SUSPENDED`，并阻断经管理器的 `send_to_module`、`broadcast` 及 `subscribe` 路由；**不**调用模块 `stop()`。
- 模块自有硬件（中断、DMA、定时器等）及管理器之外的直接调用仍可能继续运行。
- 需要调用业务 `stop()` 完全停用：须先 `module_manager_resume_module`，再 `module_manager_stop_module`；对 `SUSPENDED` 直接 `stop_module` 幂等返回 0 且不调 `stop()`。
- `module_manager_stop_all` 只停止 `RUNNING` 槽位，不包含 `SUSPENDED`。

---

## 应用启动与初始化顺序（Zephyr SYS_INIT）

本模板在 **`main()` 进入之前** 通过 Zephyr **`SYS_INIT(fn, POST_KERNEL, prio)`** 完成应用与子系统初始化；**同一 `POST_KERNEL` 阶段内，`prio` 数值越小越先执行**（与 `include/zeplod/app_config.h` 中 **`APP_INIT_PRIO_*`** 宏一致）。

### 启动阶段（`main` 之前）

典型顺序为：

1. **`app_init_apply_cb`**：打印版本、清零应用控制块、写入默认 `app_config_t`（来自 `app_config.h` 宏）。
2. **`sys_log_init` / `sys_mem_init`**（受 `APP_CONFIG_ENABLE_*` 控制）。
3. **`event_system_init`** → **`event_dispatcher_init`** → **`sys_timer_init`** → **`sys_wdt_init`**（按宏开关）。
4. **`module_manager_init`**。
5. **各业务模块**在各自 **`.c`** 内通过 **`SYS_INIT(..., APP_INIT_PRIO_MODULE_*)`** 调用 **`module_manager_register()`**（示例见 `example_module_a.c`、`example_module_ipc.c` 等）。有依赖的模块应使用**更大**的 `APP_INIT_PRIO_*`（更晚注册），例如 `example_module_multi_dep` 在 A/B 之后。
6. **`app_init_finalize`**：置位「应用已初始化」、记录启动时间戳。

失败时（如 `event_dispatcher_init` 非 `EVENT_OK`）对应 init 回调返回非 0，Zephyr 启动路径会报错；具体行为以 Zephyr 版本为准。

### `app_init()` / `app_start()`（`main` 中）

- **`app_init(config)`**：在 SYS_INIT 已全部成功后返回 **`APP_OK`**；**`config` 指针当前不参与** SYS_INIT 路径（保留 API 兼容）。若需运行时覆盖配置，应在架构上另选钩子（如弱符号默认表或独立初始化模块）。
- **`app_start()`**：仍由 **`main()`** 调用：启动事件系统、分发器线程、模块管理器及已注册模块、看门狗与心跳定时器等（与改版前行为一致）。

### 新增业务模块时如何注册

1. 在模块 **`.c`** 文件末尾增加 **`SYS_INIT(your_module_auto_register, POST_KERNEL, APP_INIT_PRIO_…)`**，在回调内准备 **`config`** 并调用 **`module_manager_register(your_module_get_interface(), &cfg, &id)`**。
2. 在 **`app_config.h`** 中新增 **`APP_INIT_PRIO_MODULE_YOURS`**，取值介于 **`APP_INIT_PRIO_MODULE_MGR`** 与 **`APP_INIT_PRIO_APP_FINAL`** 之间；若依赖其它模块，**数值应大于** 被依赖模块的优先级（更晚执行）。
3. **不必**再修改 **`app_main.c`** 的集中注册列表（已移除 **`app_register_modules()`**）。

单元测试工程（**`tests/`**）不链接 **`app_main.c`**，仍可在用例中显式调用 **`module_manager_init()`** / **`module_manager_register()`**。

---

## 核心概念

### 模块接口（Module Interface）

所有模块必须实现的标准接口：

```c
typedef struct {
    const char *name;                     // 模块名称
    uint32_t version;                     // 版本号 (MAJOR.MINOR.PATCH)
    module_priority_t priority;           // 优先级
    const char *const *depends_on;        // 运行时依赖名列表（NULL 结尾），可选；见下文
    int (*init)(void *config);            // 初始化函数
    int (*start)(void);                   // 启动函数
    int (*stop)(void);                    // 停止函数
    int (*shutdown)(void);                // 销毁函数
    module_event_handler_t on_event;      // 事件处理回调
    module_status_t (*get_status)(void);  // 获取状态
    int (*control)(int cmd, void *arg);   // 控制命令
} module_interface_t;
```

完整定义以仓库内 **`module_base.h`** 为准。

### 模块状态

```c
typedef enum {
    MODULE_STATUS_UNINITIALIZED = 0,  // 未初始化
    MODULE_STATUS_INITIALIZING,       // 初始化中
    MODULE_STATUS_INITIALIZED,        // 已初始化
    MODULE_STATUS_RUNNING,            // 运行中
    MODULE_STATUS_STOPPED,            // 已停止
    MODULE_STATUS_ERROR,              // 错误
    MODULE_STATUS_SUSPENDED           // 挂起
} module_status_t;
```

### 模块优先级

```c
typedef enum {
    MODULE_PRIORITY_LOW      = 10,   // 低优先级（最后启动）
    MODULE_PRIORITY_NORMAL   = 5,    // 普通优先级
    MODULE_PRIORITY_HIGH     = 2,    // 高优先级
    MODULE_PRIORITY_CRITICAL = 0     // 关键优先级（最先启动）
} module_priority_t;
```

### 模块信息结构

```c
typedef struct {
    const module_interface_t *interface;      // 模块接口
    void *config;                             // 配置数据
    void *internal_data;                      // 内部数据
    module_status_t status;                   // 当前状态
    uint32_t id;                              // 模块 ID
    module_event_subscription_t event_subscriptions[CONFIG_MODULE_MAX_EVENT_SUBSCRIPTIONS];  // 事件订阅（默认 8）
    uint8_t event_subscription_count;         // 订阅数量
} module_info_t;
```

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      应用层 (Application)                    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │              模块管理器 (Module Manager)              │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │   │
│  │  │  Module A   │  │  Module B   │  │  Module C   │  │   │
│  │  │  (GPIO)     │  │  (UART)     │  │  (Sensor)   │  │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │   │
│  │         │                │                │          │   │
│  │         └────────────────┼────────────────┘          │   │
│  │                          │                           │   │
│  │         ┌────────────────▼────────────────┐          │   │
│  │         │     事件系统集成接口             │          │   │
│  │         └────────────────┬────────────────┘          │   │
│  └──────────────────────────┼───────────────────────────┘   │
│                             │                               │
│                             ▼                               │
│                    ┌─────────────────┐                      │
│                    │   Event System  │                      │
│                    └─────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 配置选项

与模块管理器直接相关的 Kconfig 宏含义见下表；**全项目所有配置项**（含事件系统、Thread IPC 等）的集中说明见 **[项目配置项说明.md](../40-应用开发/42-项目配置项说明.md)**。

| 配置宏 | 含义（摘要） |
|--------|----------------|
| `CONFIG_MODULE_MANAGER` | 是否启用模块管理器 |
| `CONFIG_MAX_MODULES` | 可注册模块槽位上限 |
| `CONFIG_MODULE_INIT_TIMEOUT_MS` | `init()` 超时（注册阶段） |
| `CONFIG_MODULE_MANAGER_RUNTIME_DEPENDENCIES` | 是否按 `depends_on` 拓扑启动 / 逆序停止 |
| `CONFIG_MODULE_MANAGER_DEPENDS_LIST_MAX` | 单模块 `depends_on` 扫描上限（防死循环） |
| `CONFIG_MODULE_MANAGER_START_ALL_ABORT_ON_FAILURE` | `start_all` 是否在首次 `start` 失败后中止 |
| `CONFIG_EXAMPLE_MODULE_MULTI_DEP` | 是否编译多依赖示例模块 |

`CONFIG_MODULE_MAX_EVENT_SUBSCRIPTIONS` 在 `module_base.h` 中可通过宏覆盖（默认 8）。

`prj.conf` 示例：

```kconfig
CONFIG_MODULE_MANAGER=y
CONFIG_MAX_MODULES=16
CONFIG_MODULE_INIT_TIMEOUT_MS=1000
# CONFIG_MODULE_MANAGER_RUNTIME_DEPENDENCIES=y
```

---

## 模块配置管理工具

项目提供了 Python 脚本用于便捷管理模块的启用/禁用配置。

### 使用方法

**脚本位置**: `scripts/module_config.py`

**基本命令**:

```bash
# 查看所有可用模块
python scripts/module_config.py list

# 查看当前模块状态
python scripts/module_config.py status

# 启用示例模块
python scripts/module_config.py enable EXAMPLE_MODULE_GPIO

# 禁用示例模块
python scripts/module_config.py disable EXAMPLE_MODULE_GPIO

# 生成自定义配置文件
python scripts/module_config.py generate --output prj_custom.conf
```

### 工作流示例

**场景 1：启用可选模块**

```bash
# 1. 查看当前状态
python scripts/module_config.py status

# 2. 启用需要的模块
python scripts/module_config.py enable EXAMPLE_MODULE_GPIO

# 3. 确认配置已更新
python scripts/module_config.py status

# 4. 重新构建
west build -b nucleo_l4r5zi
```

**场景 2：创建自定义配置**

```bash
# 1. 启用/禁用模块
python scripts/module_config.py enable EXAMPLE_MODULE_UART
python scripts/module_config.py disable EXAMPLE_MODULE_GPIO

# 2. 生成自定义配置文件
python scripts/module_config.py generate --output prj_my_project.conf

# 3. 使用自定义配置构建
west build -b nucleo_l4r5zi . -- -DCONF_FILE="prj.conf;prj_my_project.conf"
```

### 配置说明

- 脚本直接修改 `prj.conf` 文件中的 `CONFIG_xxx=y/n` 配置
- 所有模块**彼此独立**，启用一个模块不会影响其他模块
- 生成的 `prj_custom.conf` 可用于不同项目配置的组合

### 注意事项

1. 修改配置后需要**重新运行 CMake 配置**（清理 build 目录或 `west build --pristine`）

---

## 完整 Kconfig 索引

事件 / 模块 / 系统服务 / Thread IPC 等**全部**应用侧配置项释义见 **[项目配置项说明.md](../40-应用开发/42-项目配置项说明.md)**。

---

## 运行时依赖（可选）

在 **`CONFIG_MODULE_MANAGER_RUNTIME_DEPENDENCIES=y`** 时，管理器会根据各模块 `depends_on` 指针数组（**以 NULL 结尾**，每项为其它模块的 `interface->name`）对 `module_manager_start_all()` 做拓扑排序，对 `module_manager_stop_all()` 做逆序；可与无依赖模块（`depends_on == NULL`）混在同一批。

| Kconfig | 含义 |
|---------|------|
| `MODULE_MANAGER_RUNTIME_DEPENDENCIES` | 开启运行时依赖排序 |
| `MODULE_MANAGER_DEPENDS_LIST_MAX` | **单个模块** `depends_on` 中最多扫描多少个名字（不含末尾 NULL），防止未正确终止时死循环；**不是**全系统模块总数，**不是**依赖链深度 |
| `MODULE_MANAGER_START_ALL_ABORT_ON_FAILURE` | 任一 `start` 失败后是否不再启动后续模块（利于依赖语义） |
| `EXAMPLE_MODULE_MULTI_DEP` | 编译并注册多依赖示例 `example_module_multi_dep`（依赖 `example_module_a` / `example_module_b`） |

实现要点（与 `module_manager.c` / `module_base.h` 一致）：

- 非法依赖或环会回退为仅按 `priority` 排序（停止侧为 priority 降序回退）。
- 宏参数名为 **`mod_name`**（勿用 `name`），避免与结构体字段 **`.name`** 在宏展开时冲突。
- 多依赖示例：`src/modules/example_module_multi_dep.c`。

---

## API 参考

### 核心 API

#### 初始化与关闭

```c
/**
 * @brief 初始化模块管理器
 * @return 0 成功，负值为错误码
 */
int module_manager_init(void);

/**
 * @brief 启动模块管理器
 * @return 0 成功，负值为错误码
 */
int module_manager_start(void);

/**
 * @brief 停止模块管理器
 * @return 0 成功，负值为错误码
 */
int module_manager_stop(void);

/**
 * @brief 关闭模块管理器（会停止并销毁所有模块）
 * @return 0 成功，负值为错误码
 */
int module_manager_shutdown(void);
```

### 模块注册 API

#### 注册与注销

```c
/**
 * @brief 注册模块
 * @param interface 模块接口指针
 * @param config 模块配置数据
 * @param module_id 输出：分配的模块 ID
 * @return 0 成功，负值为错误码
 *
 * @note init() 在模块管理器互斥锁外调用——避免在用户代码里出现
 *       需要回调管理器 API 时的死锁。如果 init 超过
 *       CONFIG_MODULE_INIT_TIMEOUT_MS，注册会失败。
 */
int module_manager_register(const module_interface_t *interface,
                            void *config,
                            uint32_t *module_id);

/**
 * @brief 注销模块
 * @param module_id 模块 ID
 * @return 0 成功，负值为错误码
 */
int module_manager_unregister(uint32_t module_id);
```

#### 模块查询

```c
/**
 * @brief 获取模块信息（线程安全快照）
 * @param module_id 模块 ID
 * @param out 输出结构体（必须非空）
 * @return 0 成功，-1 未找到或参数无效
 */
int module_manager_get_module_info(uint32_t module_id, module_info_t *out);

/**
 * @brief 通过名称获取模块 ID
 * @param name 模块名称
 * @return 模块 ID，未找到返回 0
 */
uint32_t module_manager_get_id_by_name(const char *name);

/**
 * @brief 遍历所有模块
 * @param callback 回调函数
 * @param user_data 用户数据
 *
 * @note 回调在管理器锁外执行，可调用 module_manager_* API 而不会重入死锁
 */
void module_manager_foreach(void (*callback)(module_info_t *, void *),
                            void *user_data);
```

### 模块生命周期 API

```c
/**
 * @brief 启动单个模块
 * @param module_id 模块 ID
 * @return 0 成功，负值为错误码
 */
int module_manager_start_module(uint32_t module_id);

/**
 * @brief 停止单个模块
 * @param module_id 模块 ID
 * @return 0 成功（包括非 RUNNING 状态的幂等调用），负值为错误码
 * @note 重复 stop 是安全的——若模块状态不是 RUNNING（含 SUSPENDED），返回 0 且不调业务 stop；
 *       挂起后须先 resume 再 stop 才会调用业务 stop()
 */
int module_manager_stop_module(uint32_t module_id);

/**
 * @brief 启动所有模块（按优先级排序）
 * @return 成功启动的模块数量
 */
int module_manager_start_all(void);

/**
 * @brief 停止所有模块（仅 RUNNING，不含 SUSPENDED）
 * @return 成功停止的模块数量
 */
int module_manager_stop_all(void);

/**
 * @brief 挂起模块（仅暂停管理器侧事件投递，不调用 stop）
 * @param module_id 模块 ID
 * @return 0 成功，负值为错误码
 * @note 不调 stop()；硬件中断/DMA 等仍可能活跃。完全停用须先 resume 再 stop
 */
int module_manager_suspend_module(uint32_t module_id);

/**
 * @brief 恢复挂起的模块
 * @param module_id 模块 ID
 * @return 0 成功，负值为错误码
 */
int module_manager_resume_module(uint32_t module_id);
```

### 事件处理 API

```c
/**
 * @brief 模块订阅事件类型
 * @param module_id 模块 ID
 * @param event_type 事件类型
 * @return 0 成功，负值为错误码
 */
int module_manager_subscribe(uint32_t module_id, event_type_t event_type);

/**
 * @brief 模块取消订阅事件类型
 * @param module_id 模块 ID
 * @param event_type 事件类型
 * @return 0 成功，负值为错误码
 */
int module_manager_unsubscribe(uint32_t module_id, event_type_t event_type);

/**
 * @brief 发送事件到指定模块
 * @param module_id 模块 ID
 * @param event 事件指针
 * @return 0 成功，负值为错误码
 */
int module_manager_send_to_module(uint32_t module_id, const event_t *event);

/**
 * @brief 广播事件到所有模块
 * @param event 事件指针
 * @return 接收事件的模块数量
 */
int module_manager_broadcast(const event_t *event);
```

### 统计与调试 API

```c
/**
 * @brief 获取模块管理器统计信息
 * @param stats 输出统计结构体
 */
void module_manager_get_stats(module_mgr_stats_t *stats);

/**
 * @brief 重置统计信息
 */
void module_manager_reset_stats(void);

/**
 * @brief 打印模块信息到控制台
 */
void module_manager_dump_info(void);

/**
 * @brief 注册模块事件回调
 * @param callback 回调函数
 * @param user_data 用户数据
 */
void module_manager_set_callback(module_mgr_callback_t callback,
                                  void *user_data);
```

### 兼容层 API

通过 `module_manager_compat.h` 提供 `module_compat_*` 薄封装，统一映射到标准版 `module_manager_*`（历史 PRO 后端已移出本仓库，本头文件不再切换闭源实现）。

```c
#include <zeplod/module_manager_compat.h>

module_compat_config_t cfg = {
    .max_modules = 16,
    .max_dependencies = 4,
    .enable_auto_deps = false,
    .enable_hotplug = false,
    .enable_lifecycle_hooks = false,
    .enable_health_monitor = false,
};

module_compat_init(&cfg);
module_compat_start();
```

详见头文件 `include/zeplod/module_manager_compat.h` 中的 API 列表。

### 辅助宏

以下与 **`module_base.h`** 中实现一致；形参名为 **`mod_name`**（模块前缀），**不得**使用 `name`，否则与结构体字段 `.name` 在宏展开时冲突。

```c
// 版本号编码/解码
#define MODULE_VERSION(major, minor, patch) \
    (((major) << 16) | ((minor) << 8) | (patch))

#define MODULE_VERSION_MAJOR(v)  (((v) >> 16) & 0xFF)
#define MODULE_VERSION_MINOR(v)  (((v) >> 8) & 0xFF)
#define MODULE_VERSION_PATCH(v)  ((v) & 0xFF)

// 声明完整模块接口（需要所有回调函数）
#define DECLARE_MODULE_INTERFACE(mod_name) \
    extern const module_interface_t mod_name##_interface; \
    const module_interface_t mod_name##_interface = { \
        .name = #mod_name, \
        .version = MODULE_VERSION(1, 0, 0), \
        .priority = MODULE_PRIORITY_NORMAL, \
        .depends_on = NULL, \
        .init = mod_name##_init, \
        .start = mod_name##_start, \
        .stop = mod_name##_stop, \
        .shutdown = mod_name##_shutdown, \
        .on_event = mod_name##_on_event, \
        .get_status = mod_name##_get_status, \
        .control = mod_name##_control \
    }

#define DECLARE_MODULE_INTERFACE_WITH_DEPS(mod_name, deps_array) \
    extern const module_interface_t mod_name##_interface; \
    const module_interface_t mod_name##_interface = { \
        .name = #mod_name, \
        .version = MODULE_VERSION(1, 0, 0), \
        .priority = MODULE_PRIORITY_NORMAL, \
        .depends_on = (deps_array), \
        .init = mod_name##_init, \
        .start = mod_name##_start, \
        .stop = mod_name##_stop, \
        .shutdown = mod_name##_shutdown, \
        .on_event = mod_name##_on_event, \
        .get_status = mod_name##_get_status, \
        .control = mod_name##_control \
    }

// 声明最小模块接口（可选回调为 NULL）
#define DECLARE_MODULE_INTERFACE_MINIMAL(mod_name) \
    extern const module_interface_t mod_name##_interface; \
    const module_interface_t mod_name##_interface = { \
        .name = #mod_name, \
        .version = MODULE_VERSION(1, 0, 0), \
        .priority = MODULE_PRIORITY_NORMAL, \
        .depends_on = NULL, \
        .init = mod_name##_init, \
        .start = mod_name##_start, \
        .stop = mod_name##_stop, \
        .shutdown = NULL, \
        .on_event = mod_name##_on_event, \
        .get_status = NULL, \
        .control = NULL \
    }

#define DECLARE_MODULE_INTERFACE_MINIMAL_WITH_DEPS(mod_name, deps_array) \
    extern const module_interface_t mod_name##_interface; \
    const module_interface_t mod_name##_interface = { \
        .name = #mod_name, \
        .version = MODULE_VERSION(1, 0, 0), \
        .priority = MODULE_PRIORITY_NORMAL, \
        .depends_on = (deps_array), \
        .init = mod_name##_init, \
        .start = mod_name##_start, \
        .stop = mod_name##_stop, \
        .shutdown = NULL, \
        .on_event = mod_name##_on_event, \
        .get_status = NULL, \
        .control = NULL \
    }
```

调用方式不变，例如：`DECLARE_MODULE_INTERFACE(my_module);`、`DECLARE_MODULE_INTERFACE_MINIMAL_WITH_DEPS(foo, foo_deps);`。

---

## 使用指南

### 快速开始

#### 1. 创建模块

**模块头文件 (my_module.h)**:

```c
#ifndef MY_MODULE_H
#define MY_MODULE_H

#include <zeplod/module_base.h>

// 模块配置结构
typedef struct {
    int param1;
    int param2;
} my_module_config_t;

// 模块接口函数声明
int my_module_init(void *config);
int my_module_start(void);
int my_module_stop(void);
int my_module_shutdown(void);
void my_module_on_event(const event_t *event, void *user_data);
module_status_t my_module_get_status(void);
int my_module_control(int cmd, void *arg);

// 模块特定 API
int my_module_do_something(void);

#endif /* MY_MODULE_H */
```

**模块实现文件 (my_module.c)**:

```c
#include <zeplod/my_module.h>
#include <zephyr/logging/log.h>

LOG_MODULE_REGISTER(my_module, LOG_LEVEL_INF);

// 模块静态数据
static my_module_config_t g_config;
static module_status_t g_status = MODULE_STATUS_UNINITIALIZED;

// 初始化
int my_module_init(void *config)
{
    LOG_INF("Initializing my_module");

    if (config == NULL) {
        return -1;
    }

    // 复制配置
    memcpy(&g_config, config, sizeof(g_config));

    // 执行初始化操作
    // ...

    g_status = MODULE_STATUS_INITIALIZED;
    LOG_INF("my_module initialized with param1=%d, param2=%d",
            g_config.param1, g_config.param2);

    return 0;
}

// 启动
int my_module_start(void)
{
    LOG_INF("Starting my_module");

    // 启动操作
    // ...

    g_status = MODULE_STATUS_RUNNING;
    LOG_INF("my_module started");
    return 0;
}

// 停止
int my_module_stop(void)
{
    LOG_INF("Stopping my_module");

    // 停止操作
    // ...

    g_status = MODULE_STATUS_STOPPED;
    LOG_INF("my_module stopped");
    return 0;
}

// 销毁
int my_module_shutdown(void)
{
    LOG_INF("Shutting down my_module");

    // 清理资源
    // ...

    g_status = MODULE_STATUS_UNINITIALIZED;
    LOG_INF("my_module shutdown complete");
    return 0;
}

// 事件处理
void my_module_on_event(const event_t *event, void *user_data)
{
    if (event == NULL) {
        return;
    }

    LOG_INF("my_module received event: type=%d", event->type);

    // 根据事件类型处理
    switch (event->type) {
    case 100:
        // 处理特定事件
        break;
    default:
        break;
    }
}

// 获取状态
module_status_t my_module_get_status(void)
{
    return g_status;
}

// 控制命令
int my_module_control(int cmd, void *arg)
{
    switch (cmd) {
    case 0:
        // 命令 0 处理
        break;
    default:
        return -1;
    }
    return 0;
}

// 模块特定功能
int my_module_do_something(void)
{
    if (g_status != MODULE_STATUS_RUNNING) {
        return -1;
    }
    // 执行功能
    return 0;
}

// 声明模块接口
DECLARE_MODULE_INTERFACE(my_module);
```

#### 2. 注册和使用模块

**本仓库主固件**：`module_manager_init()` 与 **`module_manager_register()`** 的调用发生在 **`SYS_INIT(POST_KERNEL, …)`** 阶段（见上文「应用启动与初始化顺序」）；下列代码演示**手动顺序**，适用于理解 API 或 **`tests/`** 等不链接 `app_main.c` 的场景。

```c
#include <zeplod/module_manager.h>
#include <zeplod/my_module.h>

void app_main(void)
{
    // 1. 初始化模块管理器
    int ret = module_manager_init();
    if (ret != 0) {
        LOG_ERR("Failed to init module manager");
        return;
    }

    // 2. 准备模块配置
    my_module_config_t config = {
        .param1 = 10,
        .param2 = 20
    };

    // 3. 注册模块
    uint32_t module_id;
    ret = module_manager_register(&my_module_interface,
                                   &config,
                                   &module_id);
    if (ret != 0) {
        LOG_ERR("Failed to register module");
        return;
    }
    LOG_INF("Module registered with ID: %u", module_id);

    // 4. 启动模块管理器
    ret = module_manager_start();
    if (ret != 0) {
        LOG_ERR("Failed to start module manager");
        return;
    }

    // 5. 启动模块（可单独启动或启动所有）
    ret = module_manager_start_module(module_id);
    // 或：module_manager_start_all();

    // 6. 订阅事件
    ret = module_manager_subscribe(module_id, EVENT_TYPE_SENSOR_DATA);
    if (ret != 0) {
        LOG_WRN("Failed to subscribe to event");
    }

    // 7. 使用模块功能
    my_module_do_something();

    // 8. 发送事件到模块
    event_t event = {
        .type = EVENT_TYPE_GENERIC,
        .priority = EVENT_PRIORITY_NORMAL,
        .data = NULL,
        .data_len = 0
    };
    module_manager_send_to_module(module_id, &event);

    // 9. 广播事件
    module_manager_broadcast(&event);
}
```

### 完整示例：GPIO 模块

#### 头文件 (example_module_gpio.h)

```c
#ifndef EXAMPLE_MODULE_GPIO_H
#define EXAMPLE_MODULE_GPIO_H

#include <zeplod/module_base.h>
#include <zephyr/drivers/gpio.h>
#include <stdint.h>
#include <stdbool.h>

// GPIO 模块配置
typedef struct {
    const struct device *led_port;
    gpio_pin_t led_pin;
    const struct device *button_port;
    gpio_pin_t button_pin;
    uint32_t blink_interval_ms;
} example_module_gpio_config_t;

// 模块接口函数
int example_module_gpio_init(void *config);
int example_module_gpio_start(void);
int example_module_gpio_stop(void);
int example_module_gpio_shutdown(void);
void example_module_gpio_on_event(const event_t *event, void *user_data);
module_status_t example_module_gpio_get_status(void);
int example_module_gpio_control(int cmd, void *arg);

// GPIO 模块特定 API
int example_module_gpio_set_led(bool on);
bool example_module_gpio_toggle_led(void);
bool example_module_gpio_get_button(void);
int example_module_gpio_set_blink_interval(uint32_t interval_ms);

// 控制命令定义
#define GPIO_CMD_SET_BLINK_INTERVAL  0
#define GPIO_CMD_GET_BLINK_INTERVAL  1

#endif /* EXAMPLE_MODULE_GPIO_H */
```

#### 实现文件 (example_module_gpio.c)

```c
#include <zeplod/example_module_gpio.h>
#include <zephyr/logging/log.h>
#include <string.h>

LOG_MODULE_REGISTER(example_module_gpio, LOG_LEVEL_INF);

// 模块私有数据
typedef struct {
    example_module_gpio_config_t config;
    module_status_t status;
    bool led_state;
    struct k_timer blink_timer;
    uint32_t blink_count;
} gpio_module_data_t;

static gpio_module_data_t g_gpio_data;

// 定时器回调
static void blink_timer_handler(struct k_timer *timer)
{
    example_module_gpio_toggle_led();
    g_gpio_data.blink_count++;
}

// 初始化
int example_module_gpio_init(void *config)
{
    LOG_INF("Initializing GPIO module");

    if (config == NULL) {
        return -EINVAL;
    }

    memcpy(&g_gpio_data.config, config, sizeof(g_gpio_data.config));
    g_gpio_data.status = MODULE_STATUS_INITIALIZING;
    g_gpio_data.led_state = false;
    g_gpio_data.blink_count = 0;

    // 初始化 LED 引脚
    if (g_gpio_data.config.led_port != NULL) {
        int ret = gpio_pin_configure(g_gpio_data.config.led_port,
                                      g_gpio_data.config.led_pin,
                                      GPIO_OUTPUT_ACTIVE);
        if (ret < 0) {
            LOG_ERR("Failed to configure LED pin");
            g_gpio_data.status = MODULE_STATUS_ERROR;
            return ret;
        }
    }

    // 初始化按钮引脚
    if (g_gpio_data.config.button_port != NULL) {
        int ret = gpio_pin_configure(g_gpio_data.config.button_port,
                                      g_gpio_data.config.button_pin,
                                      GPIO_INPUT | GPIO_PULL_UP);
        if (ret < 0) {
            LOG_ERR("Failed to configure button pin");
            g_gpio_data.status = MODULE_STATUS_ERROR;
            return ret;
        }
    }

    // 初始化定时器
    k_timer_init(&g_gpio_data.blink_timer, blink_timer_handler, NULL);

    g_gpio_data.status = MODULE_STATUS_INITIALIZED;
    LOG_INF("GPIO module initialized");
    return 0;
}

// 启动
int example_module_gpio_start(void)
{
    LOG_INF("Starting GPIO module");

    if (g_gpio_data.config.blink_interval_ms > 0) {
        k_timer_start(&g_gpio_data.blink_timer,
                      K_MSEC(g_gpio_data.config.blink_interval_ms),
                      K_MSEC(g_gpio_data.config.blink_interval_ms));
    }

    g_gpio_data.status = MODULE_STATUS_RUNNING;
    LOG_INF("GPIO module started");
    return 0;
}

// 停止
int example_module_gpio_stop(void)
{
    LOG_INF("Stopping GPIO module");

    k_timer_stop(&g_gpio_data.blink_timer);

    g_gpio_data.status = MODULE_STATUS_STOPPED;
    LOG_INF("GPIO module stopped");
    return 0;
}

// 销毁
int example_module_gpio_shutdown(void)
{
    LOG_INF("Shutting down GPIO module");

    k_timer_stop(&g_gpio_data.blink_timer);

    // 关闭 LED
    if (g_gpio_data.config.led_port != NULL) {
        gpio_pin_set(g_gpio_data.config.led_port,
                     g_gpio_data.config.led_pin, 0);
    }

    g_gpio_data.status = MODULE_STATUS_UNINITIALIZED;
    LOG_INF("GPIO module shutdown complete");
    return 0;
}

// 事件处理
void example_module_gpio_on_event(const event_t *event, void *user_data)
{
    if (event == NULL) {
        return;
    }

    LOG_DBG("GPIO module received event: type=%d", event->type);

    switch (event->type) {
    case EVENT_TYPE_SENSOR_CONFIG:
        // 处理配置事件
        if (event->data != NULL && event->data_len > 0) {
            uint8_t *data = (uint8_t *)event->data;
            // 解析并应用配置
        }
        break;

    default:
        break;
    }
}

// 获取状态
module_status_t example_module_gpio_get_status(void)
{
    return g_gpio_data.status;
}

// 控制命令
int example_module_gpio_control(int cmd, void *arg)
{
    switch (cmd) {
    case GPIO_CMD_SET_BLINK_INTERVAL:
        if (arg == NULL) {
            return -EINVAL;
        }
        g_gpio_data.config.blink_interval_ms = *(uint32_t *)arg;
        return 0;

    case GPIO_CMD_GET_BLINK_INTERVAL:
        if (arg == NULL) {
            return -EINVAL;
        }
        *(uint32_t *)arg = g_gpio_data.config.blink_interval_ms;
        return 0;

    default:
        return -ENOTSUP;
    }
}

// LED 控制
int example_module_gpio_set_led(bool on)
{
    if (g_gpio_data.status != MODULE_STATUS_RUNNING) {
        return -EPERM;
    }

    if (g_gpio_data.config.led_port != NULL) {
        gpio_pin_set(g_gpio_data.config.led_port,
                     g_gpio_data.config.led_pin, on ? 1 : 0);
        g_gpio_data.led_state = on;
        return 0;
    }
    return -ENODEV;
}

// 切换 LED 状态
bool example_module_gpio_toggle_led(void)
{
    bool new_state = !g_gpio_data.led_state;
    example_module_gpio_set_led(new_state);
    return new_state;
}

// 读取按钮
bool example_module_gpio_get_button(void)
{
    if (g_gpio_data.config.button_port != NULL) {
        return gpio_pin_get(g_gpio_data.config.button_port,
                            g_gpio_data.config.button_pin);
    }
    return false;
}

// 设置闪烁间隔
int example_module_gpio_set_blink_interval(uint32_t interval_ms)
{
    g_gpio_data.config.blink_interval_ms = interval_ms;

    if (g_gpio_data.status == MODULE_STATUS_RUNNING) {
        if (interval_ms > 0) {
            k_timer_start(&g_gpio_data.blink_timer,
                          K_MSEC(interval_ms),
                          K_MSEC(interval_ms));
        } else {
            k_timer_stop(&g_gpio_data.blink_timer);
        }
    }
    return 0;
}

// 声明模块接口
DECLARE_MODULE_INTERFACE(example_module_gpio);
```

#### 使用示例

```c
#include <zeplod/module_manager.h>
#include <zeplod/example_module_gpio.h>

void gpio_module_example(void)
{
    // 初始化模块管理器
    module_manager_init();
    module_manager_start();

    // 配置 GPIO 模块
    example_module_gpio_config_t gpio_config = {
        .led_port = DEVICE_DT_GET(DT_NODELABEL(led0)),
        .led_pin = 0,
        .button_port = DEVICE_DT_GET(DT_NODELABEL(sw0)),
        .button_pin = 0,
        .blink_interval_ms = 500
    };

    // 注册模块
    uint32_t gpio_module_id;
    module_manager_register(&example_module_gpio_interface,
                            &gpio_config,
                            &gpio_module_id);

    // 启动模块
    module_manager_start_module(gpio_module_id);

    // 订阅事件
    module_manager_subscribe(gpio_module_id, EVENT_TYPE_SENSOR_DATA);

    // 使用模块 API
    example_module_gpio_set_led(true);
    example_module_gpio_toggle_led();

    // 通过控制命令修改配置
    uint32_t new_interval = 1000;
    example_module_gpio_control(GPIO_CMD_SET_BLINK_INTERVAL, &new_interval);

    // 获取模块信息
    module_info_t info;
    module_manager_get_module_info(gpio_module_id, &info);
    LOG_INF("Module: %s, Status: %d", info.interface->name, info.status);

    // 打印所有模块信息
    module_manager_dump_info();

    // 获取统计信息
    module_mgr_stats_t stats;
    module_manager_get_stats(&stats);
    LOG_INF("Total modules: %u, Active: %u",
            stats.total_modules, stats.active_modules);
}
```

### 模块间通信

```c
// 模块 A 发送事件到模块 B
void module_a_send_to_module_b(void)
{
    uint32_t module_b_id = module_manager_get_id_by_name("module_b");

    event_t event = {
        .type = EVENT_TYPE_GENERIC,
        .priority = EVENT_PRIORITY_NORMAL,
        .source_id = g_my_module_id,
        .reserved = 0,
        .data.ptr = &some_data,
        .data_len = sizeof(some_data)
    };

    module_manager_send_to_module(module_b_id, &event);
}

// 广播事件到所有模块
void broadcast_to_all_modules(void)
{
    event_t event = {
        .type = EVENT_TYPE_SENSOR_DATA,
        .priority = EVENT_PRIORITY_HIGH,
        .reserved = 0,
        .data.ptr = &sensor_data,
        .data_len = sizeof(sensor_data)
    };

    int count = module_manager_broadcast(&event);
    LOG_INF("Event sent to %d modules", count);
}
```

### 模块遍历

```c
// 回调函数
void print_module_info(module_info_t *info, void *user_data)
{
    LOG_INF("Module: %s, ID: %u, Status: %d",
            info->interface->name, info->id, info->status);
}

// 遍历所有模块
void list_all_modules(void)
{
    module_manager_foreach(print_module_info, NULL);
}
```

---

## 最佳实践

### 1. 模块设计原则

```c
// ✅ 推荐：模块化设计，职责单一
typedef struct {
    // 只包含必要的配置
    int param;
} simple_module_config_t;

// ❌ 避免：模块过于庞大
typedef struct {
    // 过多的配置项
    int param1, param2, ..., param50;
} complex_module_config_t;
```

### 2. 初始化注意事项

```c
// ✅ 推荐：快速初始化，延迟启动
int my_module_init(void *config)
{
    // 只做必要的初始化
    // 不要启动线程、定时器等
    return 0;
}

int my_module_start(void)
{
    // 在这里启动线程、定时器等
}

// ❌ 避免：在 init 中做过多工作
int bad_init(void *config)
{
    k_thread_create(...);  // 应该在 start 中做
    k_timer_start(...);    // 应该在 start 中做
    return 0;
}
```

### 3. 事件处理最佳实践

```c
// ✅ 推荐：快速处理事件
void my_module_on_event(const event_t *event, void *user_data)
{
    // 快速复制数据
    memcpy(&local_data, event->data, MIN(event->data_len, sizeof(local_data)));

    // 触发信号量，让工作线程处理
    k_sem_give(&work_sem);
}

// ❌ 避免：在事件回调中执行耗时操作
void bad_on_event(const event_t *event, void *user_data)
{
    k_msleep(1000);  // 阻塞事件调度
    complex_calculation();  // 耗时计算
}
```

### 4. 错误处理

```c
// ✅ 推荐：完整的错误处理
int my_module_init(void *config)
{
    if (config == NULL) {
        g_status = MODULE_STATUS_ERROR;
        return -EINVAL;
    }

    int ret = some_init_operation();
    if (ret < 0) {
        g_status = MODULE_STATUS_ERROR;
        LOG_ERR("Init failed: %d", ret);
        return ret;
    }

    g_status = MODULE_STATUS_INITIALIZED;
    return 0;
}

// ❌ 避免：忽略错误
int bad_init(void *config)
{
    some_init_operation();  // 不检查返回值
    g_status = MODULE_STATUS_INITIALIZED;
    return 0;
}
```

### 5. 资源管理

```c
// ✅ 推荐：完整的资源清理
int my_module_shutdown(void)
{
    // 停止所有活动
    k_timer_stop(&timer);
    k_thread_abort(&thread);

    // 释放资源
    k_free(dynamic_memory);

    // 重置状态
    g_status = MODULE_STATUS_UNINITIALIZED;
    return 0;
}
```

### 6. 模块优先级设置

```c
// 关键模块（先启动）
const module_interface_t sensor_interface = {
    .name = "sensor",
    .priority = MODULE_PRIORITY_CRITICAL,  // 传感器最先启动
    // ...
};

// 普通模块
const module_interface_t logger_interface = {
    .name = "logger",
    .priority = MODULE_PRIORITY_NORMAL,
    // ...
};

// 低优先级模块（后启动）
const module_interface_t stats_interface = {
    .name = "statistics",
    .priority = MODULE_PRIORITY_LOW,
    // ...
};
```

---

## 故障排除

### 常见问题

#### 1. 模块注册失败

**症状**：`module_manager_register` 返回错误

**检查清单**：
- [ ] 模块接口是否正确定义？
- [ ] 是否超过最大模块数？
- [ ] init 函数是否超时？

```c
// 调试步骤
void debug_register_failure(void)
{
    module_mgr_stats_t stats;
    module_manager_get_stats(&stats);

    LOG_INF("Total: %u, Max: %d",
            stats.total_modules, CONFIG_MAX_MODULES);

    if (stats.error_modules > 0) {
        LOG_ERR("Some modules failed to initialize");
    }
}
```

#### 2. 模块启动失败

**症状**：`module_manager_start_module` 返回错误

**可能原因**：
- 模块状态不是 INITIALIZED 或 STOPPED
- start 函数返回错误

```c
// 检查模块状态
void check_module_state(uint32_t module_id)
{
    module_info_t info;
    if (module_manager_get_module_info(module_id, &info) == 0) {
        LOG_INF("Module %s status: %d", info.interface->name, info.status);
    }
}
```

#### 3. 事件未送达模块

**症状**：模块未收到订阅的事件

**检查清单**：
- [ ] 模块是否已启动（RUNNING 状态）？
- [ ] 是否正确订阅了事件类型？
- [ ] on_event 回调是否非空？

```c
// 检查订阅状态
void check_subscription(uint32_t module_id, event_type_t event_type)
{
    module_info_t info;
    module_manager_get_module_info(module_id, &info);

    LOG_INF("Module %s subscriptions: %u",
            info.interface->name, info.event_subscription_count);

    for (uint8_t i = 0; i < info.event_subscription_count; i++) {
        if (info.event_subscriptions[i].type == event_type) {
            LOG_INF("Subscribed to event type %u", event_type);
            return;
        }
    }
    LOG_WRN("Not subscribed to event type %u", event_type);
}
```

#### 4. 模块化陷阱

**症状**：系统行为异常（卡死、状态错误、崩溃）

**陷阱一：状态机陷阱**

```c
// ❌ 错误示例 - 状态机非法转换
int bad_init(void *config)
{
    // 在 init 中调用 stop 会触发非法状态转换
    module_manager_stop(my_module_id);
    return 0;
}

// ✅ 正确示例
int good_init(void *config)
{
    // 只做初始化，不操作状态机
    return 0;
}
```

**陷阱二：依赖陷阱**

如果模块 A 依赖模块 B，但运行时依赖未正确声明，单独 stop 模块 B 会导致依赖它的下游悬空：

```c
// 正确做法：声明依赖关系
static const char * const my_deps[] = {"other_module", NULL};
DECLARE_MODULE_INTERFACE_WITH_DEPS(my_module, my_deps);
```

**陷阱三：回调重入陷阱**

在 module 事件回调链中再次调用 `module_manager_register` 或订阅新事件，可能触发 list 内部容量扩展（realloc），导致迭代器失效：

```c
// ❌ 危险示例
static void my_module_on_event(const event_t *event, void *user_data)
{
    if (some_condition) {
        // 危险：可能在 list 扩展期间被调用
        event_subscribe(OTHER_EVENT, another_callback, NULL, &sub_id);
    }
}
```

### 错误码说明

| 错误码 | 描述 |
|--------|------|
| 0 | 成功 |
| -1 | 通用错误/未找到 |
| -EINVAL | 无效参数 |
| -EPERM | 权限错误（状态不正确） |
| -ENOTSUP | 不支持的操作 |
| -ENODEV | 设备不存在 |
| -ENOMEM | 内存不足 |

### 调试技巧

```c
// 打印模块信息
void debug_modules(void)
{
    module_manager_dump_info();
}

// 打印统计信息
void debug_stats(void)
{
    module_mgr_stats_t stats;
    module_manager_get_stats(&stats);

    LOG_INF("=== Module Manager Stats ===");
    LOG_INF("Total modules: %u", stats.total_modules);
    LOG_INF("Active modules: %u", stats.active_modules);
    LOG_INF("Error modules: %u", stats.error_modules);
    LOG_INF("Events processed: %u", stats.events_processed);
    LOG_INF("Events dropped: %u", stats.events_dropped);
}
```

---

## 附录

### 头文件包含

```c
#include <zeplod/module_manager.h>  // 模块管理器 API
#include <zeplod/module_base.h>     // 模块基础接口和宏
```

### 示例模块

项目包含以下示例模块：
- `example_module_gpio` - GPIO 控制模块
- `example_module_uart` - UART 通信模块
- `example_module_a` - 通用示例模块
- `example_module_b` - 通用示例模块

### 相关文档

- [事件系统详细使用说明.md](31-事件系统详细使用说明.md)
- [开发者入门指南.md](../00-入门/04-开发者入门指南.md)
- [Zephyr RTOS 官方文档](https://docs.zephyrproject.org/)

> 语言: **中文** | [English](../../en/40-app-development/43-config-comparison-guide.md)

# 配置方案对比指南

本文档对比四种配置方案的内存占用和适用场景。

---

## 📊 配置方案对比表

| 配置项 | **prj.conf** (标准版) | **conf/profiles/balanced.conf** (平衡版) | **conf/profiles/minimal.conf** (极简版) | **conf/profiles/tiny.conf** (极限版) |
|--------|----------------------|---------------------------|--------------------------|---------------------------|
| **目标 SRAM** | ≥ 256KB | 64-128KB | 32-64KB | **≤ 32KB** |
| **框架占用** | ~190KB | ~40KB | ~18KB | **< 10KB** |
| **APP 可用** | ~66KB+ | ~24-88KB | ~14-46KB | **> 22KB** |
| **适用场景** | 资源充足 MCU | 中等资源 MCU | 极受限 MCU | **极限资源 MCU** |
| **示例板型** | STM32H7/NRF52840 | STM32L4/NRF52832 | STM32F103C8T6 | **32KB SRAM 系统** |

---

## 🔧 核心配置对比

### 1. 事件系统

| 配置项 | 标准版 | 平衡版 | 极简版 | 极限版 |
|--------|--------|--------|--------|--------|
| `CONFIG_EVENT_MAX_TYPES` | 256 | 128 | 64 | **8** |
| `CONFIG_EVENT_MAX_SUBSCRIBERS` | 16 | 8 | 4 | **1** |
| `CONFIG_EVENT_QUEUE_SIZE` | 64 | 32 | 16 | **4** |
| `CONFIG_EVENT_DISPATCHER_STACK_SIZE` | 2048 | 1536 | 1024 | **256** |
| **内存占用** | ~73KB | ~36KB | ~18KB | **~1KB** |

### 2. 系统内存池

| 配置项 | 标准版 | 平衡版 | 极简版 | 极限版 |
|--------|--------|--------|--------|--------|
| `CONFIG_SYS_MEMORY_ENABLE` | y | y | y | **n** |
| `CONFIG_SYS_MEMORY_POOL_SIZE` | 8192 | 4096 | 4096 | **0** |
| 内存跟踪 | 256 条 | 64 条 | 禁用 | **禁用** |
| **内存占用** | ~38KB | ~12KB | ~4KB | **0** |

### 3. 栈配置

| 配置项 | 标准版 | 平衡版 | 极简版 | 极限版 |
|--------|--------|--------|--------|--------|
| `CONFIG_MAIN_STACK_SIZE` | 4096 | 3072 | 2048 | **512** |
| `CONFIG_ISR_STACK_SIZE` | 2048 | 2048 | 1024 | **512** |
| `CONFIG_IDLE_STACK_SIZE` | 320 | 320 | 256 | **128** |
| `CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE` | 2048 | 1536 | 1024 | **256** |
| `CONFIG_EVENT_DISPATCHER_STACK_SIZE` | 2048 | 1536 | 1024 | **256** |

### 4. Heap 配置

| 配置项 | 标准版 | 平衡版 | 极简版 | 极限版 |
|--------|--------|--------|--------|--------|
| `CONFIG_HEAP_MEM_POOL_SIZE` | 65536 | 16384 | 4096 | **1024** |
| `CONFIG_KERNEL_MEM_POOL` | y | y | y | **y** |

### 5. 系统服务

| 配置项 | 标准版 | 平衡版 | 极简版 | 极限版 |
|--------|--------|--------|--------|--------|
| `CONFIG_SYS_LOG_ENABLE` | y | y | n | **n** |
| `CONFIG_SYS_TIMER_ENABLE` | y | y | y | **n** |
| `CONFIG_SYS_WATCHDOG_ENABLE` | y | y | n | **n** |
| `CONFIG_WATCHDOG` | y | y | n | **n** |

### 6. 其他配置

| 配置项 | 标准版 | 平衡版 | 极简版 | 极限版 |
|--------|--------|--------|--------|--------|
| `CONFIG_SHELL` | ✓ | ✓ | ✗ | **✗** |
| `CONFIG_CONSOLE` | ✓ | ✓ | ✗ | **✗** |
| `CONFIG_SERIAL` | ✓ | ✓ | ✗ | **✗** |
| `CONFIG_PRINTK` | ✓ | ✓ | ✓ | **✗** |
| `CONFIG_LOG` | ✓ | ✓ | ✗ | **✗** |
| `CONFIG_ASSERT` | ✓ | ✓ | ✗ | **✗** |
| `CONFIG_MAX_MODULES` | 16 | 12 | 8 | **2** |
| `CONFIG_APP_KV_ENABLE` | y | y | n | **n** |
| `CONFIG_EXAMPLE_MODULE_A_ENABLE` | y | y | n | **n** |
| `CONFIG_EXAMPLE_MODULE_B_ENABLE` | y | n | n | **n** |

---

## 🎯 内存占用详细对比

### 标准版 (~190KB)

> ⚠️ **注意**：标准版事件系统占用较大（73KB），适合 SRAM ≥ 256KB 的 MCU

```
事件系统:        73.0KB  █████████████████████████████████████████
内存池:          38.0KB  ██████████████████████████████
Heap:            64.0KB  ████████████████████████████████████████████████
定时器:          35.0KB  ██████████████████████████
日志系统:         8.0KB  ██████
模块管理器:      10.0KB  ████████
应用层:           2.0KB  ██
内核栈/其他:      10.0KB  ████████
─────────────────────────────────
总计:         ~190KB
```

### 平衡版 (~40KB)

```
事件系统:        36.0KB  ████████████████████████████████
内存池:          12.0KB  ████████████
Heap:            16.0KB  ████████████████
定时器:          16.0KB  ████████████████
日志系统:         8.0KB  ████████
模块管理器:       7.5KB  ███████
应用层:           2.0KB  ██
内核栈/其他:      6.5KB  ██████
─────────────────────────────────
总计:          ~40KB
```

### 极简版 (~18KB)

```
事件系统:        18.0KB  ████████████████████
内存池:           4.0KB  ████████
Heap:             4.0KB  ████████
定时器:           5.0KB  ██████████
日志系统:         2.0KB  ████
模块管理器:       5.0KB  ██████████
应用层:           1.0KB  ██
内核栈/其他:      3.0KB  ██████
─────────────────────────────────
总计:          ~18KB  ✅
```

### 极限版 (<10KB) ⭐ 推荐 32KB SRAM 系统

```
事件系统:         1.0KB  ██
内存池:           0.0KB
Heap:             1.0KB  ██
定时器:           0.0KB
日志系统:         0.0KB
模块管理器:       0.5KB  █
应用层:           0.5KB  █
ISR栈:            0.5KB  █
主线程栈:         0.5KB  █
分发器栈:         0.25KB
空闲线程栈:       0.125KB
内核其他:         1.0KB  ██
─────────────────────────────────
总计:          ~5-6KB  ✅✅✅
```

**极限版内存分配（32KB SRAM 系统）**：
- 框架占用：**< 10KB**
- APP 可用：**> 22KB**

---

## 📝 使用方法

### 构建标准版（默认）

```bash
west build -b mimxrt1050_fire/mimxrt1052/qspi .
# 或显式指定
west build -b mimxrt1050_fire/mimxrt1052/qspi . -DCONF_FILE="prj.conf"
```

### 构建平衡版

```bash
west build -b mimxrt1050_fire/mimxrt1052/qspi . -DCONF_FILE="prj.conf;conf/profiles/balanced.conf"
```

### 构建极简版

```bash
west build -b mimxrt1050_fire/mimxrt1052/qspi . -DCONF_FILE="prj.conf;conf/profiles/minimal.conf"
```

### 构建极限版 ⭐ 32KB SRAM 系统

```bash
west build -b mimxrt1050_fire/mimxrt1052/qspi . -DCONF_FILE="prj.conf;conf/profiles/tiny.conf" --pristine
```

> **注意**：极限版适用于 SRAM 仅 32KB 的系统，框架占用 < 10KB，预留 22KB+ 给 APP 模块。

### 验证内存占用

```bash
# 构建后查看内存使用
west build -t mem_report

# 或查看映射文件
cat build/zephyr/zephyr.map | grep -A 5 "Memory Configuration"

# 查看 BSS 段大对象
cat build/zephyr/zephyr.map | grep "\.bss\." | grep -v "0x0"
```

---

## ⚠️ 功能限制说明

### 极限版限制（conf/profiles/tiny.conf）

使用 `conf/profiles/tiny.conf` 时，以下功能**不可用**：

| 功能 | 状态 | 说明 |
|------|------|------|
| Shell | ❌ 禁用 | 无交互调试 |
| Console | ❌ 禁用 | 无控制台输出 |
| Serial | ❌ 禁用 | 无串口驱动 |
| Printk | ❌ 禁用 | 无打印输出 |
| LOG | ❌ 禁用 | 无日志系统 |
| Assert | ❌ 禁用 | 无断言检查 |
| 看门狗 | ❌ 禁用 | 无看门狗保护 |
| 定时器服务 | ❌ 禁用 | 无软件定时器 |
| 内存池 | ❌ 禁用 | 无 sys_memory |
| KV 存储 | ❌ 禁用 | 无键值存储 |
| 示例模块 | ❌ 禁用 | 无示例代码 |

**事件系统限制**：

| 配置项 | 标准版 | 极限版 |
|--------|--------|--------|
| 事件类型数 | 256 | **8** |
| 每类型订阅者 | 16 | **1** |
| 事件队列深度 | 64 | **4** |
| 最大模块数 | 16 | **2** |

**栈大小限制**：

| 栈类型 | 标准版 | 极限版 |
|--------|--------|--------|
| 主线程栈 | 4096 | **512** |
| ISR 栈 | 2048 | **512** |
| 空闲线程栈 | 320 | **128** |
| 分发器栈 | 2048 | **256** |
| Workqueue 栈 | 2048 | **256** |

### 极简版限制

使用 `conf/profiles/minimal.conf` 时，以下功能**不可用**：

- ❌ Shell 交互命令
- ❌ 内存泄漏跟踪
- ❌ 事件/分发器统计
- ❌ 日志转储命令
- ❌ 看门狗保护
- ❌ KV 存储
- ❌ 示例模块 A/B
- ❌ 日志彩色输出
- ❌ 断言检查

### 平衡版限制

使用 `conf/profiles/balanced.conf` 时，以下功能**受限**：

- ⚠️ 最大 128 种事件类型（标准版 256）
- ⚠️ 每类型最多 8 个订阅者（标准版 16）
- ⚠️ 内存池仅 2 个（标准版 4 个）
- ⚠️ 最大 16 个定时器（标准版 32）
- ⚠️ 无运行时统计（`APP_ENABLE_STATS=n`）
- ⚠️ 看门狗仅软件模式

---

## 🔍 如何选择

### 选择标准版的场景

- ✅ MCU SRAM ≥ 256KB
- ✅ 需要完整调试功能
- ✅ 需要硬件看门狗
- ✅ 需要内存泄漏跟踪
- ✅ 需要 Shell 交互调试
- ✅ 事件类型 > 128 种

### 选择平衡版的场景

- ✅ MCU SRAM 64-128KB
- ✅ 需要基本调试功能
- ✅ 软件看门狗足够
- ✅ 事件类型 ≤ 128 种
- ✅ 订阅者 ≤ 8 个/类型
- ✅ 需要 Shell 但可放弃统计

### 选择极简版的场景

- ✅ MCU SRAM 32-64KB
- ✅ 生产环境（无需调试）
- ✅ 事件类型 ≤ 64 种
- ✅ 订阅者 ≤ 4 个/类型
- ✅ 无需交互调试
- ✅ 资源受限

### 选择极限版的场景 ⭐ 推荐

- ✅ **MCU SRAM ≤ 32KB**
- ✅ **需要预留 ≥ 22KB 给 APP 模块**
- ✅ 生产环境（完全无调试）
- ✅ 事件类型 ≤ 8 种
- ✅ 订阅者 ≤ 1 个/类型
- ✅ 无需日志、Shell、看门狗、定时器
- ✅ 极简事件驱动架构

---

## 🛠️ 自定义配置

如需进一步调整，可创建自己的叠加配置文件：

```bash
# 复制模板
cp conf/profiles/tiny.conf conf/profiles/custom.conf

# 编辑配置
vim conf/profiles/custom.conf

# 构建时使用
west build -b mimxrt1050_fire/mimxrt1052/qspi . -DCONF_FILE="prj.conf;conf/profiles/custom.conf"
```

### 关键配置项速查

| Kconfig 选项 | 作用 | 最小值 | 极限版 | 极简版 | 推荐值 |
|-------------|------|--------|--------|--------|--------|
| `CONFIG_EVENT_MAX_TYPES` | 事件类型数 | 4 | **8** | 64 | 128-256 |
| `CONFIG_EVENT_QUEUE_SIZE` | 事件队列深度 | 4 | **4** | 16 | 32-64 |
| `CONFIG_EVENT_MAX_SUBSCRIBERS` | 每类型订阅者数 | 1 | **1** | 4 | 8-16 |
| `CONFIG_EVENT_DISPATCHER_STACK_SIZE` | 分发器栈 | 256 | **256** | 1024 | 1024-2048 |
| `CONFIG_MAX_MODULES` | 最大模块数 | 1 | **2** | 8 | 8-16 |
| `CONFIG_SYS_MEMORY_POOL_SIZE` | 内存池大小 | 0 | **0** | 4096 | 4096-8192 |
| `CONFIG_HEAP_MEM_POOL_SIZE` | Heap 大小 | 512 | **1024** | 4096 | 4096-16384 |
| `CONFIG_MAIN_STACK_SIZE` | 主线程栈 | 512 | **512** | 2048 | 2048-4096 |
| `CONFIG_ISR_STACK_SIZE` | ISR 栈 | 512 | **512** | 1024 | 2048 |
| `CONFIG_IDLE_STACK_SIZE` | 空闲栈 | 128 | **128** | 256 | 320 |

---

## 📌 极限配置优化技巧

### 1. 条件编译

极限配置下，以下模块会自动跳过编译：

- `sys_log.c` - 当 `CONFIG_SYS_LOG_ENABLE=n`
- `sys_memory.c` - 当 `CONFIG_SYS_MEMORY_ENABLE=n`
- `sys_watchdog.c` - 当 `CONFIG_SYS_WATCHDOG_ENABLE=n`
- `sys_timer.c` - 当 `CONFIG_SYS_TIMER_ENABLE=n`
- `example_module_*.c` - 当 `CONFIG_EXAMPLE_MODULE_*_ENABLE=n`

### 2. 栈大小优化

栈大小是内存占用的大头，极限配置的关键优化：

```properties
# 最小可行的栈配置
CONFIG_MAIN_STACK_SIZE=512      # 主线程
CONFIG_ISR_STACK_SIZE=512       # 中断栈
CONFIG_IDLE_STACK_SIZE=128      # 空闲线程
CONFIG_EVENT_DISPATCHER_STACK_SIZE=256  # 事件分发器
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=256  # 工作队列
```

### 3. 事件系统优化

事件系统是框架核心，合理配置可大幅节省内存：

```properties
# 极限配置
CONFIG_EVENT_MAX_TYPES=8        # 仅支持 8 种事件
CONFIG_EVENT_MAX_SUBSCRIBERS=1  # 每种事件仅 1 个订阅者
CONFIG_EVENT_QUEUE_SIZE=4       # 队列深度仅 4
```

### 4. 禁用调试输出

完全禁用输出可节省大量代码和数据空间：

```properties
CONFIG_PRINTK=n          # 禁用打印
CONFIG_CONSOLE=n         # 禁用控制台
CONFIG_SERIAL=n          # 禁用串口
CONFIG_LOG=n             # 禁用日志
CONFIG_ASSERT=n          # 禁用断言
```

---

## 📌 注意事项

1. **配置文件叠加顺序**：`prj.conf` → `prj_xxx.conf`（后者覆盖前者）
2. **Kconfig range 限制**：某些选项有最小值限制，见 `Kconfig` 文件
3. **内存计算**：仅包含静态分配，不含 Zephyr 内核和动态分配
4. **构建验证**：修改配置后务必 `--pristine` 重新构建
5. **实际测量**：使用 `CONFIG_INIT_STACKS=y` 可查看栈使用情况
6. **栈溢出风险**：极限栈配置可能导致复杂回调溢出，需谨慎测试

---

## 🔧 极限配置调优指南

### 栈大小调整原则

| 回调复杂度 | 推荐 Dispatcher 栈 | 推荐 Main 栈 |
|-----------|-------------------|-------------|
| 极简（无嵌套调用） | 256 | 512 |
| 简单（少量函数） | 512 | 1024 |
| 中等（有日志输出） | 1024 | 2048 |
| 复杂（多层嵌套） | 2048 | 4096 |

### 事件队列深度选择

| 场景 | 推荐深度 | 说明 |
|------|---------|------|
| 事件稀少（<10/秒） | 4 | 极限配置 |
| 事件中等（10-100/秒） | 8-16 | 极简配置 |
| 事件密集（>100/秒） | 32-64 | 标准配置 |

---

*文档版本: 1.2*
*更新日期: 2026-04-13*
*新增：极限版详细配置说明、条件编译机制、栈优化技巧*
